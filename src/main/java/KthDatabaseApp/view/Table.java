package KthDatabaseApp.view;

public class Table {
    private static final int MAX_LIST_OUTPUT_LENGTH = 600;
    private int printedLength = 0;

    /**
     * Prints the headings for the table with the specified format and a separation line.
     * @param format
     * @param headings
     */
    public void printHeadings(String format, String... headings) {
        
    }

    /**
     * Prints a row of the table using the format string and element values. If the max list output length has been reached, nothing happens.
     * @param format
     * @param elements
     * @return boolean that indicates whether the max list output length has been reached
     */
    public boolean printRow(String format, Object... elements) {

    }
}
